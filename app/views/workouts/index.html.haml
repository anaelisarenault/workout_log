
- @workouts.each do |workout|
  %h2= link_to workout.date, workouts_path
  %h3= workout.workout
  %p= workout.mood
  %p= workout.length

  = link_to "Voltar", root_path
  = link_to "Editar", edit_workout_path(@workouts)