#index_workouts
  - @workouts.each do |workout|
    %h2= link_to workout.date.strftime("%A %B %m %Y"), workout
    %h3
      %span Treino:
      = workout.workout 
    
    %div
      = link_to "Editar", edit_workout_path(@workouts), class: "btnEditar"